# Displays a message to the console
print("Hello, World!")
# Determines if a user-provided integer is even or odd
number = int(input("Enter a number: "))

if number % 2 == 0:
    print(f"{number} is Even.")
else:
    print(f"{number} is Odd.")
# Calculates the factorial of a non-negative integer using a recursive function
def factorial(n):
    if n == 0 or n == 1:
        return 1
    return n * factorial(n - 1)

num = 5
print(f"The factorial of {num} is {factorial(num)}.")
class Solution:
    def winnerSquareGame(self, n: int) -> bool:
        # dp[i] represents whether the player whose turn it is can win with i stones remaining
        dp = [False] * (n + 1)
        
        # Precompute all non-zero square numbers up to n
        squares = [k * k for k in range(1, int(n**0.5) + 1)]
        
        for i in range(1, n + 1):
            for sq in squares:
                if sq > i:
                    break
                # If taking 'sq' stones leaves the opponent in a losing position (dp[i - sq] == False),
                # then the current player can force a win.
                if not dp[i - sq]:
            
